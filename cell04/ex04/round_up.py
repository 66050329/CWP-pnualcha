#!/usr/bin/env python3
#รันปัดเศษขึ้นเป็นจำนวนเต็ม
import math

# รับค่าตัวเลขจากผู้ใช้และแปลงเป็นประเภท float
number = float(input("Give me a number: "))

# ปัดเศษขึ้นเป็นจำนวนเต็มและแสดงผล
print(math.ceil(number))